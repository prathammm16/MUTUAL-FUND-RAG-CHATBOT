# Railway / Nixpacks — production image (no tests, no PyTorch)
[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements-prod.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 1"
